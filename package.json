{
  "name": "regolo-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "postinstall": "pnpm --filter @regolo/cli link --global"
  }
}
